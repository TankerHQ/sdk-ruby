# frozen_string_literal: true

source 'https://rubygems.org'

# Add runtime dependencies directly to the tanker-core.gemspec file. They will
# be loaded in development, test, and ci too via the following call:
gemspec

# Add development, test, and ci dependencies below, they won't be declared, or
# loaded when the gem is published:
gem 'bundler', '~> 2.0'
gem 'bundler-audit', '~> 0.7'
gem 'faraday-net_http_persistent', '~> 2.1'
gem 'rake', '~> 13.0'
gem 'rspec', '~> 3.0'
gem 'rubocop', '~> 1.13'
gem 'rubygems-tasks', '~> 0.2.6'
gem 'tanker-identity', '~> 3.3.0'
